import React from 'react';

const Pending = () => (
  <div style={{ padding: '2rem' }}>
    <h1>Account Pending</h1>
    <p>Your instructor account credentials are pending verification by the admin. Please wait.</p>
  </div>
);

export default Pending;